<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="custom_transaction_status_inherit_sepay" inherit_id="payment_custom.custom_transaction_status">
        <xpath expr="//t[@t-set='qr_code']" position="replace"/>
        <xpath expr="//div[@name='o_payment_status_alert']" position="inside">
            <t t-if="tx.payment_method_code == 'sepay' and tx.state == 'pending' and tx.company_id.sudo().partner_id.bank_ids">
                <t t-set="qr_code" t-value="tx.company_id.sudo().partner_id.bank_ids[0].build_vietqr_code(tx.amount, tx.reference)"/>
            </t>
            <t t-else="">
                <t t-set="qr_code" t-value="tx.provider_id.sudo().qr_code and tx.company_id.sudo().partner_id.bank_ids[:1].build_qr_code_base64(tx.amount, tx._get_communication(), None, tx.currency_id, tx.partner_id)"/>
            </t>
            <t t-if="tx.provider_id.sudo().code == 'custom' and qr_code">
                <div class="position-relative order-2 d-flex flex-md-column justify-content-center align-items-center align-items-md-stretch w-100 w-md-auto">
                    <hr class="d-inline d-md-none w-100"/>
                    <div class="vr d-none d-md-block h-100 mx-auto"/>
                    <h6 class="my-1 my-md-3 px-3 px-md-0 text-muted">OR</h6>
                    <hr class="d-inline d-md-none w-100"/>
                    <div class="vr d-none d-md-block h-100 mx-auto"/>
                </div>
                <div class="o_qr_code_card card order-1 order-md-3 bg-info">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center pb-3">
                        <img class="mb-2 border border-dark rounded" t-att-width="'150px' if tx.payment_method_code == 'sepay' else ''" t-att-src="qr_code"/>
                        <small class="text-center text-wrap lh-sm">Scan me in your banking app</small>
                    </div>
                </div>
            </t>
        </xpath>
    </template>
</odoo>
